# OPH Compression Scorecard

The headline of this surface: zero continuous dials by construction. Each
declared P map has an interval-certified unique fixed point, with the
gauge-width map landing 2.5×10⁻⁶ relative from the measured α⁻¹ while the
transport term is open, and the conditional capacity bridge sits about 2.5
one-dimensional Planck standard deviations from the Λ-located capacity,
conditional on F and CP-1 to CP-3. The counting below prices everything else.

The evidential accounting of the program under
[STRANGE_LOOP_PRINCIPLES.md](STRANGE_LOOP_PRINCIPLES.md) rule 4. This file carries three
things: the zero-dial statement, the zero-dial forward record, and the strict
exact-closure count M − K. This file is the manually maintained seed; CI generation from
the claim registry is fix item SLP-05. The proof spine is [PROOF_SPINE.md](PROOF_SPINE.md).

## Dials: 0

A dial is a quantity an author can adjust to fit data. The declared maps carry no
continuous dial. Each of the two incomplete P maps has an interval-certified unique fixed
point
(`../code/P_derivation/runtime/p_interval_contraction_certificate_2026-07-14.json`), but
Ward-projected transport is open. N is defined by N = F(N), with F specified in
`../code/capacity_readback/F_READBACK_SPEC.md`; its construction is conditional on
G2-GAP-1 and CP-1 to CP-3 (CL-7). Zero continuous dials does not make every displayed
number a physical test. The map, observable, convention, and uncertainty packet must also
be complete.

The discrete structural selections listed below are choices, and choices carry menu
sizes. They are counted. None of them is a continuous dial.

## The zero-dial forward record

Values emitted by declared chains, next to their comparison references. Rows evaluated at
the SL-3 working pixel say so in their condition tags. These entries have different
dependencies, selection histories, and epistemic status. They are not independent events
and do not define a statistically meaningful aggregate or coincidence price. A pull is
reported only when emission and target are the same physical observable in the same
convention and the uncertainty packet is complete.

| Quantity | Forced chain | Measured | Accuracy | Condition tag |
|---|---|---|---|---|
| α⁻¹ | 136.994835177413 (certified fixed point of the incomplete source map) | 137.035999177(21) | 3.0×10⁻⁴ diagnostic gap | Ward-projected hadronic transport absent (CL-1); frozen target exists, but the V1 source is non-blind and exploratory |
| m_W chart coordinate | 80.330 GeV | 80.3692(133) GeV, stale PDG 2025 mass-dependent-width Breit-Wigner parameter | not evaluable | not a pole target and no common observable map; no pull or evidence claim (CL-5); as a convention diagnostic, the chart coordinate sits 0.5 propagated experimental standard deviations from the PDG 2026 complex pole |
| m_Z chart coordinate | 91.119 GeV | 91.1880(20) GeV, stale PDG 2025 mass-dependent-width Breit-Wigner parameter | not evaluable | not a pole target and no common observable map; no pull or evidence claim (CL-5) |
| Hierarchy relation α_U·log(N/π) = 6π/P | 11.5546 | nominal 11.5573 at the Λ-readout N | nominal 2.3×10⁻⁴ | conditional on F and CP-1 to CP-3; Planck-posterior propagation pending (CL-3/CL-4) |
| Record capacity N | 3.53×10¹²² (conditional electroweak bridge) | nominal 3.31×10¹²² (Λ readout) | nominal 6.6×10⁻² | conditional mismatch: F and CP-1 to CP-3 are open, and the joint Planck posterior has not been propagated (CL-3/CL-7) |
| Λ_QCD^(3) | 0.3348 GeV | 0.338(12) GeV | ≈1×10⁻² | conditional on declared threshold inputs, bracket-swept; threshold provenance listed in the particle paper audit ledger |
| m_N | 0.929 GeV | 0.9389 GeV | 1.1×10⁻² | compare-only: source scale times a published lattice ratio, an external theory input whose calibration ancestry includes measured hadronic scale-setting |
| m_t (criticality branch) | 172.63 GeV | 172.60(30) GeV | 2×10⁻⁴ | conditional criticality family; CF1/CF2 are the D11 census, which consumed the measured top/Higgs pair; the three-loop implied scale is the registered discriminating test |
| n_s | 0.9660215 | 0.9649(42) | 1.2×10⁻³ | conditional analytic screen branch; retrospective comparison; formula selection counted in the inventory below (2 candidates) |
| Photon, gluon, graviton mass | 0 | bounds consistent with 0 | exact | structural zeros on the stated action branches |
| Hypercharge lattice, N_c = 3, N_g = 3 | exact SM quotient | exact | exact | conditional on declared MAR clause inputs |

Reading rule for this table: the row count is descriptive bookkeeping, not a hit count.
The rows share inputs and formulas, several consume measured basin locations, and some
are conditional or retrospective. Each open item maps to a generator in
[CONSISTENCY_STACK.md](CONSISTENCY_STACK.md). The G1 target is frozen, but no qualifying
target-blind payload has been executed; the required 4×10⁻⁹ relative payload precision
exceeds every method on Earth (see "Why The Hadronic Test Is Hard" in
[OPH_FALSIFICATION_PROGRAM.md](OPH_FALSIFICATION_PROGRAM.md)), and existing grid work is
exploratory. G3 has no physical comparison until the
readout and target share an observable convention.

## Exact-closure accounting (strict): inputs K

Working values currently consumed by chains while the exact closures stay open. These are
provenance entries. None is a dial.

Continuous settings:

| Setting | Located from | Value |
|---|---|---|
| P (working value) | CODATA α (SL-3) | 1.630968209403959… |
| N (working value) | Planck Λ + scale bridge (SL-4) | 3.31×10¹²² |
| SI bridge | cesium ν (SL-5) | γ★ |

The SI bridge carries the dimensionful displays. G and ħ ride it the way c and the SI
second ride every physical theory: a dimensionless theory can only display dimensionful
numbers through unit anchors. G is therefore untestable as a number in any theory; OPH's
gravitational content is the dimensionless capacity relation tested through CL-3.

Basin-locating measurements additionally consumed by current chains (each an input where
it appears on a dependency path): frozen W/Z pair (calibration lane), PDG Higgs/top (D11
interpolation lane), PDG masses used as anchors in flavor code, Planck cosmology
parameters (dark-sector likelihood loop).

Discrete structural selections (choices with menu sizes; from the trials inventory):
e^(−2π) and P^(1/6) in M_U; E_P/√P for E_cell; β_EW = N_c+1 (the β_EW
alternatives were checked only inside the declared chart-coordinate sweep); P/4 pixel
budget; frozen MSSM one-loop coefficients (the SM triple has no pixel-residual root in
the declared scan; this is not a physical W/Z exclusion); ×2 write/check orientation
(12→24); δ_e = 2/9; n_e = (7,4,3); 2^(1/6); reserve fraction e^(−P/24) (selected of 9
candidates against SPARC); n_s formula (2 circulating candidates); birefringence
expression (4th of 4 tried); quark denominator tuple (minimum of 219,615-member grammar,
self-rejected); criticality boundary scale (log-midpoint of a 6-pair bracketing family); electroweak
value-law two-way selection (ambiguity 9.3 MeV on M_W, 5.1 MeV on M_Z).

## Exact-closure accounting (strict): outputs M

Landed basins under rule 4, meaning exact landings inside measured basins by chains the
basins did not tune:

| Quantity | Status |
|---|---|
| Photon, gluon, graviton mass = 0 | structural zeros on the stated action branches; landed |
| Hypercharge lattice (given MAR clauses as inputs) | landed, conditional on the declared clause inputs |

Rows that do NOT qualify under rule 4 at current standing, and why: α (working value via SL-3; the
declared source maps omit the Ward-projected hadronic transport); Λ (working value via
SL-4); exact W/Z, Higgs/top (consumed their targets); forward W/Z (physical comparison
not evaluable: CL-5); n_s (formula selection counted, 2
candidates; κ_rep pending; not lattice-derived); RAR/a₀ (function fitted to the comparison data;
inherits measured H₀, Λ); charged-lepton ppm rows (fitted phase δ=2/9, excluded at ≈440σ
as prediction); neutrino candidate (rejected by NuFIT 6.1); χ_ν (no closure; bridge
absent); m_H/m_t D11 (interpolation, admitted in code).

## Current standing

Dials: zero by the stated construction. Exact-closure count M − K: negative until the
generators land. The table does not support a scoreboard aggregate or coincidence price.
There are zero discriminating frozen-prospective hits. The IBM S₃ check is
non-discriminating because standard quantum mechanics predicts the same programmed
observable, and the electroweak repair packets use known data and are not prospective.
The historical G1 v2 target is externally timestamped but algebraically
invalid; corrective v3 is a permanently inactive post-target-access erratum
scaffold. Its V1 source is non-blind and earns no promotion. Every future claim
of compression cites this file.
