{
  "artifact": "oph_hybrid_ir_bracket_envelope_spec",
  "format_version": 1,
  "date_utc": "2026-07-16",
  "label": "compare_only_non_blind",
  "row_class": "diagnostic_non_promoting",
  "physical_claim": false,
  "declaration": {
    "statement": "Every analysis choice below (ensembles, measurement definitions, matching rule, fit windows, Z_V rule, envelope factor magnitudes, combination rule, bracket construction, containment rule) is declared in this file and hashed BEFORE the lattice moments, the hybrid interval, or the containment against S_required are evaluated. This file is not edited after the first evaluation; any forced amendment would be recorded as a separate file with both hashes.",
    "frozen_targets_untouched": [
      "ward_projected_payload/ (read-only import)",
      "falsification/ (untouched)"
    ]
  },
  "frozen_inputs": {
    "payload_bracket_artifact": "ward_projected_payload/runtime/ward_projected_payload_bracket_current.json",
    "payload_bracket_content_sha256": "053db6566bd7d8d7a889e40bd7148495ab9f388a79cc67638ac0c88639479754",
    "old_s_effective_bracket": {"lo": 0.5577788584117769, "hi": 1.0543469546225994},
    "s_required_compare_only_non_blind": "0.895400132647658797805800283181670641",
    "pass_tolerance_alpha_inv": 2.1e-08,
    "pass_tolerance_relative_on_delta_had": 4.036126488247091e-09,
    "evaluation_point": "payload_harness.build_evaluation_point(precision=40), Stage-5 internal chain, no external empirical inputs"
  },
  "ensembles": {
    "A": {
      "ensemble_id": "hybrid_A_quenched_b5p7_16x4c3",
      "shape_TXYZ": [16, 4, 4, 4],
      "beta": 5.7,
      "kappa": 0.15,
      "c_sw": 1.0,
      "n_therm": 100,
      "n_sep": 10,
      "n_cfg": 64,
      "n_or": 1,
      "cg_tol": 1e-08,
      "seed": 716001
    },
    "B": {
      "ensemble_id": "hybrid_B_quenched_b5p7_24x6c3",
      "shape_TXYZ": [24, 6, 6, 6],
      "beta": 5.7,
      "kappa": 0.15,
      "c_sw": 1.0,
      "n_therm": 100,
      "n_sep": 10,
      "n_cfg": 20,
      "n_or": 1,
      "cg_tol": 1e-08,
      "seed": 716002
    },
    "central_rule": "Central S_IR is taken from ensemble B (larger volume, longer time extent). Ensemble A is the statistics workhorse and cross-check; the A-versus-B spread is reported. If B fails to complete inside the compute budget, A becomes central and that substitution is stated in the artifact.",
    "thermalization_rule": "Plaquette recorded every thermalization sweep; thermalization is accepted if the last 20 thermalization sweeps show no drift beyond 2 sigma of the measurement-chain plaquette scatter.",
    "autocorrelation_rule": "Integrated autocorrelation time of the plaquette and of the per-configuration TMR moment over the measurement chain, Madras-Sokal window at 4*tau; jackknife errors are inflated by sqrt(2*tau_int) of the moment series when tau_int > 0.5."
  },
  "measurement": {
    "correlator": "Transverse local-local vector correlator, polarizations GAMMA[1],GAMMA[2],GAMMA[3] (the temporal channel GAMMA[0] is excluded; the pre-existing demo contraction averaged GAMMA[0..2] and thereby carried a factor 2/3 dilution, corrected here), single flavor, unit charge, color trace included.",
    "folding": "G(t) symmetrized about T/2 as in vector_correlator.fold_correlator.",
    "kernel": "t^2 kernel (a*mZ -> infinity limit of the contract kernel), moment M = 4*pi*sum_{t=0}^{T/2} G_folded(t)*t^2, per vector_correlator.tmr_moment(amz=None).",
    "hopping_normalization": "Physical moment = hopping moment / (2*kappa)^2 for each lattice measurement (interacting and free reference).",
    "z_v_rule": "Z_V^eff(t) = C_CL(t)/(2*kappa*C_LL(t)) with C_CL the conserved(sink)-local(source) transverse correlator (exactly conserved point-split Noether current; Ward identity verified numerically off-source). Z_V = plain mean of Z_V^eff(t) over the plateau window t in [3, T/2-1] on folded correlators; per-t values outside (0.2, 1.2) are flagged in the artifact, never silently altered. The renormalized interacting moment is Z_V^2 * M_LL.",
    "vector_mass_rule": "a*m_V from the folded transverse correlator, effective mass log(G(t)/G(t+1)), plain mean over the declared window t in [3, T/2-1] restricted to the contiguous positive-correlator range starting at t=3; jackknife over configurations.",
    "free_reference_rule": "Free-field (cold gauge) lattice measurement on the SAME shape and c_SW, at kappa_free fixed by the matching rule below, same contraction, same folding, same kernel window, physical normalization /(2*kappa_free)^2. An infinite-volume continuum free-parton reference is reported as context only and enters nothing (finite-volume phase-space discreteness makes it incommensurable at these volumes).",
    "matching_rule": "2 * am_q_free = a*m_V(measured), with the free Wilson pole mass am_q(kappa) = log(1 + 1/(2*kappa) - 4); kappa_free = 0.5/(exp(am_V/2) - 1 + 4).",
    "s_ir_definition": "S_IR = [Z_V^2 * M_LL / (2*kappa)^2] / [M_free(kappa_free) / (2*kappa_free)^2]. Statistical error by jackknife over configurations with the free-reference m_V dependence propagated linearly through dM_free/dm_V measured from free solves at m_V +/- 0.05 in lattice units.",
    "pion_correlator": "Measured and reported for context (a*m_PS); enters no number in the bracket."
  },
  "envelope": {
    "form": "S_IR interval = S_IR_central * [1 - E_total, 1 + E_total], with S_IR_lo clamped at 0.0 from below (the below-cutoff spectral density is nonnegative).",
    "combination_rule": "E_total = quadrature sum sqrt(sum_i e_i^2) of the declared relative factors below plus the measured statistical relative error.",
    "factors": {
      "e_stat": "measured jackknife relative error on S_IR of the central ensemble (rule, not a number; inflated per the autocorrelation rule)",
      "e_z_v": {"value": 0.30, "motivation": "Z_V^2 from the conserved-local estimator carries O(a) local-conserved contamination and excited-state pollution at the coarse coupling g^2 = 6/5.7; nonperturbative-vs-perturbative Z_V determinations for coarse Wilson/clover actions differ at the 10-15 percent level on Z_V, taken generously as 30 percent on Z_V^2."},
      "e_quenching": {"value": 0.15, "motivation": "quenched versus 2+1-flavor vector-channel spectral quantities differ at the 10-15 percent level in literature comparisons of light vector observables and HVP-type moments."},
      "e_discretization": {"value": 0.25, "motivation": "single coarse lattice spacing at beta 5.7 with tree-level c_SW = 1 and hopping (2*kappa) normalization in place of a tadpole-improved scheme; generous single-spacing envelope, no continuum limit."},
      "e_finite_volume": {"value": 0.20, "motivation": "spatial extents 4^3 and 6^3 in lattice units; free-reference matching cancels tree-level phase-space discreteness only partially."},
      "e_transfer": {"value": 0.30, "motivation": "duality transfer: the interacting-over-free moment ratio is measured at the heavy demo mass point on the full lattice spectral support and applied to the contract's below-cutoff light-quark region [y_thr, y0]; covers the mass-point mismatch and the support mismatch."},
      "e_kernel": {"value": 0.02, "motivation": "t^2 kernel in place of the a*mZ contract kernel (relative 4/(a*mZ)^2 for t >= 1 on any coarse lattice) plus the finite-T truncation of the TMR sum at T/2."}
    }
  },
  "bracket_construction": {
    "grid": "The frozen pqcd grid exactly as declared_grid() in ward_projected_payload/run_bracket.py defines it: Lambda3 in {lane_lo, lane_central, lane_hi} x k_cut in {2, 4, 8} x order in {1, 2, 3}. The above-cutoff treatment is untouched: Delta_above(Lambda3, k, order) = Delta_had of the frozen module pqcd(below='zero') via payload_harness.emit_delta_source with gauss_n=48, splits_per_decade=4.",
    "ir_replacement": "Delta_IR_free(Lambda3, k) = sum_q parton_moment(y_thr_q, N_c Q_q^2, y_lo=y_thr_q, y_hi=y0) with y0 = (k*Lambda3/mZ)^2, i.e. exactly the frozen below='free' minus below='zero' difference. The below-cutoff dichotomy factor {0, 1} is replaced by the measured interval: S_eff_new = (Delta_above + S_IR * Delta_IR_free) / Delta_quark_naive, evaluated at S_IR in {S_IR_lo, S_IR_hi}.",
    "row_exclusions": "parton_free and constituent reference branches of the old bracket are not rows of the hybrid bracket; their role (spanning the IR treatment) is superseded by the measured IR interval. Stated here before evaluation.",
    "bracket": "hybrid s_effective interval = [min, max] over the 54 evaluated values (27 grid points x 2 envelope edges); consistency identity Delta_zero + 1.0*Delta_IR_free = Delta_free is checked against the frozen artifact rows to 1e-9 before use."
  },
  "containment_rule": {
    "comparison": "compare_only_non_blind",
    "check": "S_required inside [s_lo, s_hi] of the hybrid bracket; also reported: distance from S_required to the nearest edge, hybrid width versus old width, hybrid width in Delta_source alpha^-1 units versus the 2.1e-8 pass tolerance, and the equivalent relative width on Delta_had versus the 4.04e-9 relative pass tolerance.",
    "honesty_clause": "If the interval is wider than the old bracket or excludes S_required, that outcome is reported as-is."
  }
}
